# 2026.252.0536 — docs: precise two-workflow naming + live-proven run recipe

## Summary
Docs-only change (the branch was rebased onto origin/main so the diff is README.md + docs/eval-charly.md ONLY):
- README.md: unambiguous framing (the OMARCHY PR-eval lane — golden-VM behavior verification of omacom/omarchy PRs, verdicts PASS | FAIL | NO_VALIDATION) with the explicit "Not to be confused with" block for the opencharly org PR validator (plugin-review/action-review, PASS|BLOCK).
- docs/eval-charly.md: the corrected placement story (the entity loader is placement-independent — CHARLY_PLUGIN_DIR + CHARLY_BIN; no baked/compiled-in requirement) + the live-proven run recipe.

## How tested
Docs-only — no code/config changed in this PR. The referenced live 11-PR batch and the deterministic verdict fix are separate PRs (plugin-pipeline #4, eval-omarchy #46).

## Rulebook compliance
- R1: the prior stale claim (compiled-in placement) corrected with the live evidence.
- R2: only live-backed claims.
- R3/R4/R5/R8/R9: n/a for a docs-only diff.
- R6: PR-only; the branch was rebased onto the current main (the diff no longer carries the merged #2 config).
- R0/R10: n/a — docs.

## Change classification
Docs-only; verification gate: the repo CI (no code); attribution tier: documentation reviewed.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*