# 2026.251.2243 — fix: lane gates to the implemented probes; ignore generated beds

## Summary
- charly.yml: the lane `gates:` list now names the implemented probes (`sequencing`, `lock_audit`) — the earlier `zero-lock`/`teardown` names weren't probes (live-pilot finding).
- .gitignore: generated `pr-beds/`, `stage-findings.yml` run artifacts.

## How tested
- `charly box validate`: OK — 186 candies, 42 boxes, 3 deploys, 0 warnings (released charly v2026.251 + baked plugin-pipeline).
- `charly pipeline validate eval-lane-plan`: valid (12 stages).
- Live lane run: legs 1-4 green (channel-select / triage / bed-render / config-audit); VM legs hard-fail honestly on a host without libvirt (bed unresolved).

## Rulebook compliance
R1: fix driven by the live pilot failure; PR-only landing.

## Change classification
Fix — config; verification gate: live pilot run; attribution tier: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*