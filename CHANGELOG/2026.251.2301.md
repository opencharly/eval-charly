# 2026.251.2301 — fix(lane): the check leg's bed fully resolvable in-tree

## Summary
The lane's check leg is now fully resolvable in-tree (the earlier red-probe "bed unresolved" failure RCA'd to two in-tree defects):
- `bed_template`: schema header (`version:`), the resolved golden ref (`@channel-select.golden` from the channel registry -> the bed's `from:`), the RED-PROBE twin bed (apply-marker known-red step; the eval bed stamps the marker post-apply, so the twin is RED pre-apply / GREEN post-apply), and `pr-apply` args now render from the plan vars.
- Channels registry: bare drive-form golden refs (reference parity).
- `discover:` loads `pr-beds/` and `eval/` (generated beds + reports are project members).
- `.gitignore` accounts for `pr-beds/`, `stage-findings.yml`, `/medium/` (all three, matching the file).
- engine (plugin-pipeline #2): `resolve_channel` returns the registry entry; probe outputs expose `golden`/`provision`.

## How tested
- `charly box validate` (released charly v2026.251 + baked plugin): OK — 186 candies, 42 boxes, 3 deploys, 0 warnings (pasted above in the repo docs).
- Lane legs 1-4 live green: channel-select / triage (local Ollama `deepseek-v4-flash:cloud`, real gh PR tools, skills corpus) / bed-render (generated bed carries `version:` + the resolved golden + the probe twin) / config-audit.
- The VM check leg is a NAMED BATCH: `omarchy-eval-vm-leg-2026.251` — operator eval host (libvirt + the `check-omarchy-eval-base-inst` golden, eval-omarchy project context). Run `charly pipeline run eval-lane-plan --pr <N>` there; the RED-PROBE exit-2 contract is executed and recorded there. NOT claimed validated on this sandbox (no libvirt host).

## Rulebook compliance
- R1 (RCA every anomaly): yes — the bed-unresolved failure RCA'd to the missing probe twin + unresolved `from:` var; both fixed in-tree; the VM leg is a named batch, not an excuse.
- R2 (no false conclusions): VM leg conclusions only after the named-batch run.
- R3 (one canonical implementation): the bed shapes mirror the eval-omarchy reference beds; no parallel definitions.
- R4 (no workarounds): no sleeps/retries/pins; the schema floor + discovery are the mechanism.
- R5 (delete legacy in the same PR): n/a — cutover is a later PR (post-pilot), explicitly not included.
- R6 (git safety): no force-push to main, no hook bypass, PR-only landing.
- R8 (no magic numbers / documented constants): pins documented in the plan.
- R9 (no hidden behavior): every stage's contract is in the entity, validated by box validate.
- R0 (skills first): engine docs + omarchy-eval skills consumed; skill refs tested live.
- R10 (fresh disposable proof): pilot ran from the committed tree; the VM-leg proof is the named batch.

## Change classification
Fix — config/lane + engine contract; verification gate: live pilot + box validate; attribution tier: analysed on a live system (VM leg pending the named batch).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*